# 코스트코 → 네이버 스마트스토어 자동 등록

이 저장소에는 코스트코 소싱 상품을 네이버 스마트스토어에 자동 등록하는 워크플로우가 있다.

**상품 등록 관련 작업(상품 올리기, 등록, 가격 계산, 이미지 가공, listing 생성)을 요청받으면
반드시 `.claude/skills/smartstore-listing/SKILL.md`를 먼저 읽고 그 지침을 그대로 따라라.**
워크플로우 세부 규칙(자동 연속 처리, 역마진 방지, 상품명 SEO 공식, sanity check)은 모두 그 파일에만 정의되어 있다 — 여기서 요약을 재해석하지 말 것.

요점만 짧게:
- 사용자가 상품 폴더를 지정하고 시작을 지시하면 그것이 등록 승인이다. 상품별 재확인 없이 등록까지 완료하고 마지막에 결과를 일괄 보고한다.
- 판매가는 반드시 `scripts/pricing.py`로 계산하며, 역마진/목표마진(매출의 10%) 미달 상품은 등록하지 않고 스킵한다.
- 결정적 작업(가격/서명/이미지/API 호출)은 `scripts/`의 파이썬 스크립트를 사용하고, 카피라이팅·카테고리 판단은 SKILL.md 지침대로 AI가 수행한다.

최초 설정(API 앱 발급, IP 등록, 의존성 설치)은 `README.md` 참고.

---

이 저장소에는 별도 트랙으로 경쟁사 상세페이지 크롤러(`crawler.py`, `extract_from_cdp.py`)와
Gemini 웹 UI 기반 상세페이지 이미지 생성기(`gemini_analyzer.py`, `detail_page_generator.py`)도 있다.
이들은 로컬 PC의 Whale/Chrome CDP 세션이 필요하며 자동 등록 워크플로우와는 독립적이다.
